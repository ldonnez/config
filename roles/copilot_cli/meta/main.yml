---
galaxy_info:
  role_name: copilot_cli
  author: ldonnez
  description: Your AI accelerator for every workflow, from the editor to the enterprise.
  license: MIT
  min_ansible_version: "2.10"

  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: macOS
      versions:
        - all

dependencies:
  - role: dotfiles
  - role: packages
  - role: gnome_keyring # Ensures access tokens of Github (after authenticating) is stored securely.
